console.error("Direct desktop development is disabled because it uses the production Comma identity.");
console.error("Run the guarded canonical launcher from apps/imsg: bun run dev:desktop");
process.exit(1);
